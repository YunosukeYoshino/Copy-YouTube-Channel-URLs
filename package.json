{
  "name": "copy-youtube-urls",
  "version": "0.1.0",
  "description": "Chrome extension that copies visible YouTube channel video URLs to the clipboard.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.200",
    "happy-dom": "^20.0.10",
    "typescript": "^5.5.6",
    "vitest": "^1.4.5"
  }
}
